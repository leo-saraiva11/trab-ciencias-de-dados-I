# === Dados brutos (baixados do Kaggle — ver README / baixar_dados.py) ===
datasets/

# === processed/ é leve (~4.7 MB) e vai para o repo, para o grupo rodar
#     treinar.py sem precisar baixar os dados brutos. Apenas o datasets/
#     bruto (acima) fica de fora por ser grande demais. ===

# HTML intermediário do relatório (o .md e o .pdf vão para o repo)
relatorio_final.html

# === Credenciais — NUNCA versionar ===
kaggle.json
.kaggle/
*.env
.env

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# === Sistema ===
.DS_Store
Thumbs.db
